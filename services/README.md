# Backend Services 

All backend services are stored in this folder.

## Subfolders

- `api-gateway/` – API gateway service
- `app/` – User app service
- `builder/` – Builder service
- `dev/` – Developer service
- `marketplace/` – Marketplace service
- `server/` – All Services in one